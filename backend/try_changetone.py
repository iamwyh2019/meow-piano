import librosa
from scipy.io import wavfile
import numpy as np
import matplotlib.pyplot as plt
import os

a = np.array([1,2,10])
b = np.array([1,2,3,4])
c = []
c.append(a)
c.append(b)
print(np.array(c).shape)

b.reshape(12)
print(b)

